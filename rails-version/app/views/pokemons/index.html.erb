<p style="color: green"><%= notice %></p>

<h1>Pokemons</h1>

<div id="pokemons">
  <%= render partial: 'pokemons/pokemon',
             collection: @pokemons,
             cached: true %>
</div>

<%= link_to "New pokemon", new_pokemon_path %>
